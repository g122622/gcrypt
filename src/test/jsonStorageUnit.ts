// import { createNewData, getData, init, setData } from '../api/core/adapters/gcryptV1/jsonStorage'
// import { log } from '../utils/gyConsole'

// export default function test() {
//     init("E:/暂存/新建文件夹 (17)/test.json", "pwd").then(() => {
//         createNewData(Buffer.from("gy3")).then(() => {
//             createNewData(Buffer.from("gy4")).then((hash) => {
//                 setData(hash, Buffer.from("gygygy5")).then(() => {
//                     log(getData(hash).toString())
//                 })
//             })
//         })
//     })
// }
